extends ../layout

block content
  h1 All Users

  if (users)
    ul
      for user in users
        a(href="/users/" + user._id)
          li= user.email + user.totalHours
  
  a(href="/users/new") new user


